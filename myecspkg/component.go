package myecspkg

import comp "bar/components"

type ComponentID uint64


var storePosition [][]comp.Position
var storeVelocity [][]comp.Velocity
var storeA [][]comp.A
var storeB [][]comp.B
var storeC [][]comp.C
var storeD [][]comp.D
var storeE [][]comp.E
var storeF [][]comp.F
var storeG [][]comp.G
var storeH [][]comp.H
var storeI [][]comp.I
var storeJ [][]comp.J
var storeK [][]comp.K
var storeL [][]comp.L
var storeM [][]comp.M
var storeN [][]comp.N
var storeO [][]comp.O
var storeP [][]comp.P
var storeQ [][]comp.Q
var storeR [][]comp.R
var storeS [][]comp.S
var storeT [][]comp.T
var storeU [][]comp.U
var storeV [][]comp.V
var storeW [][]comp.W
var storeX [][]comp.X
var storeY [][]comp.Y
var storeZ [][]comp.Z
var storeAA [][]comp.AA
var storeAB [][]comp.AB
var storeAC [][]comp.AC
var storeAD [][]comp.AD
var storeAE [][]comp.AE
var storeAF [][]comp.AF
var storeAG [][]comp.AG
var storeAH [][]comp.AH
var storeAI [][]comp.AI
var storeAJ [][]comp.AJ
var storeAK [][]comp.AK
var storeAL [][]comp.AL
var storeAM [][]comp.AM
var storeAN [][]comp.AN
var storeAO [][]comp.AO
var storeAP [][]comp.AP
var storeAQ [][]comp.AQ
var storeAR [][]comp.AR
var storeAS [][]comp.AS
var storeAT [][]comp.AT
var storeAU [][]comp.AU
var storeAV [][]comp.AV
var storeAW [][]comp.AW
var storeAX [][]comp.AX
var storeAY [][]comp.AY
var storeAZ [][]comp.AZ
var storeBA [][]comp.BA
var storeBB [][]comp.BB
var storeBC [][]comp.BC
var storeBD [][]comp.BD
var storeBE [][]comp.BE
var storeBF [][]comp.BF
var storeBG [][]comp.BG
var storeBH [][]comp.BH
var storeBI [][]comp.BI
var storeBJ [][]comp.BJ
var storeBK [][]comp.BK
var storeBL [][]comp.BL
var storeBM [][]comp.BM
var storeBN [][]comp.BN
var storeBO [][]comp.BO
var storeBP [][]comp.BP
var storeBQ [][]comp.BQ
var storeBR [][]comp.BR
var storeBS [][]comp.BS
var storeBT [][]comp.BT
var storeBU [][]comp.BU
var storeBV [][]comp.BV
var storeBW [][]comp.BW
var storeBX [][]comp.BX
var storeBY [][]comp.BY
var storeBZ [][]comp.BZ
var storeCA [][]comp.CA
var storeCB [][]comp.CB
var storeCC [][]comp.CC
var storeCD [][]comp.CD
var storeCE [][]comp.CE
var storeCF [][]comp.CF
var storeCG [][]comp.CG
var storeCH [][]comp.CH
var storeCI [][]comp.CI
var storeCJ [][]comp.CJ
var storeCK [][]comp.CK
var storeCL [][]comp.CL
var storeCM [][]comp.CM
var storeCN [][]comp.CN
var storeCO [][]comp.CO
var storeCP [][]comp.CP
var storeCQ [][]comp.CQ
var storeCR [][]comp.CR
var storeCS [][]comp.CS
var storeCT [][]comp.CT
var storeCU [][]comp.CU
var storeCV [][]comp.CV
var storeCW [][]comp.CW
var storeCX [][]comp.CX
var storeCY [][]comp.CY
var storeCZ [][]comp.CZ
var storeDA [][]comp.DA
var storeDB [][]comp.DB
var storeDC [][]comp.DC
var storeDD [][]comp.DD
var storeDE [][]comp.DE
var storeDF [][]comp.DF
var storeDG [][]comp.DG
var storeDH [][]comp.DH
var storeDI [][]comp.DI
var storeDJ [][]comp.DJ
var storeDK [][]comp.DK
var storeDL [][]comp.DL
var storeDM [][]comp.DM
var storeDN [][]comp.DN
var storeDO [][]comp.DO
var storeDP [][]comp.DP
var storeDQ [][]comp.DQ
var storeDR [][]comp.DR
var storeDS [][]comp.DS
var storeDT [][]comp.DT
var storeDU [][]comp.DU
var storeDV [][]comp.DV
var storeDW [][]comp.DW
var storeDX [][]comp.DX
var storeDY [][]comp.DY
var storeDZ [][]comp.DZ
var storeEA [][]comp.EA
var storeEB [][]comp.EB
var storeEC [][]comp.EC
var storeED [][]comp.ED
var storeEE [][]comp.EE
var storeEF [][]comp.EF
var storeEG [][]comp.EG
var storeEH [][]comp.EH
var storeEI [][]comp.EI
var storeEJ [][]comp.EJ
var storeEK [][]comp.EK
var storeEL [][]comp.EL
var storeEM [][]comp.EM
var storeEN [][]comp.EN
var storeEO [][]comp.EO
var storeEP [][]comp.EP
var storeEQ [][]comp.EQ
var storeER [][]comp.ER
var storeES [][]comp.ES
var storeET [][]comp.ET


const PositionID ComponentID = 1

const VelocityID ComponentID = 2

const AID ComponentID = 4

const BID ComponentID = 8

const CID ComponentID = 16

const DID ComponentID = 32

const EID ComponentID = 64

const FID ComponentID = 128

const GID ComponentID = 256

const HID ComponentID = 512

const IID ComponentID = 1024

const JID ComponentID = 2048

const KID ComponentID = 4096

const LID ComponentID = 8192

const MID ComponentID = 16384

const NID ComponentID = 32768

const OID ComponentID = 65536

const PID ComponentID = 131072

const QID ComponentID = 262144

const RID ComponentID = 524288

const SID ComponentID = 1048576

const TID ComponentID = 2097152

const UID ComponentID = 4194304

const VID ComponentID = 8388608

const WID ComponentID = 16777216

const XID ComponentID = 33554432

const YID ComponentID = 67108864

const ZID ComponentID = 134217728

const AAID ComponentID = 268435456

const ABID ComponentID = 536870912

const ACID ComponentID = 1073741824

const ADID ComponentID = 2147483648

const AEID ComponentID = 4294967296

const AFID ComponentID = 8589934592

const AGID ComponentID = 17179869184

const AHID ComponentID = 34359738368

const AIID ComponentID = 68719476736

const AJID ComponentID = 137438953472

const AKID ComponentID = 274877906944

const ALID ComponentID = 549755813888

const AMID ComponentID = 1099511627776

const ANID ComponentID = 2199023255552

const AOID ComponentID = 4398046511104

const APID ComponentID = 8796093022208

const AQID ComponentID = 17592186044416

const ARID ComponentID = 35184372088832

const ASID ComponentID = 70368744177664

const ATID ComponentID = 140737488355328

const AUID ComponentID = 281474976710656

const AVID ComponentID = 562949953421312

const AWID ComponentID = 1125899906842624

const AXID ComponentID = 2251799813685248

const AYID ComponentID = 4503599627370496

const AZID ComponentID = 9007199254740992

const BAID ComponentID = 18014398509481984

const BBID ComponentID = 36028797018963968

const BCID ComponentID = 72057594037927936

const BDID ComponentID = 144115188075855872

const BEID ComponentID = 288230376151711744

const BFID ComponentID = 576460752303423488

const BGID ComponentID = 1152921504606846976

const BHID ComponentID = 2305843009213693952

const BIID ComponentID = 4611686018427387904

const BJID ComponentID = -9223372036854775808

const BKID ComponentID = 0

const BLID ComponentID = 0

const BMID ComponentID = 0

const BNID ComponentID = 0

const BOID ComponentID = 0

const BPID ComponentID = 0

const BQID ComponentID = 0

const BRID ComponentID = 0

const BSID ComponentID = 0

const BTID ComponentID = 0

const BUID ComponentID = 0

const BVID ComponentID = 0

const BWID ComponentID = 0

const BXID ComponentID = 0

const BYID ComponentID = 0

const BZID ComponentID = 0

const CAID ComponentID = 0

const CBID ComponentID = 0

const CCID ComponentID = 0

const CDID ComponentID = 0

const CEID ComponentID = 0

const CFID ComponentID = 0

const CGID ComponentID = 0

const CHID ComponentID = 0

const CIID ComponentID = 0

const CJID ComponentID = 0

const CKID ComponentID = 0

const CLID ComponentID = 0

const CMID ComponentID = 0

const CNID ComponentID = 0

const COID ComponentID = 0

const CPID ComponentID = 0

const CQID ComponentID = 0

const CRID ComponentID = 0

const CSID ComponentID = 0

const CTID ComponentID = 0

const CUID ComponentID = 0

const CVID ComponentID = 0

const CWID ComponentID = 0

const CXID ComponentID = 0

const CYID ComponentID = 0

const CZID ComponentID = 0

const DAID ComponentID = 0

const DBID ComponentID = 0

const DCID ComponentID = 0

const DDID ComponentID = 0

const DEID ComponentID = 0

const DFID ComponentID = 0

const DGID ComponentID = 0

const DHID ComponentID = 0

const DIID ComponentID = 0

const DJID ComponentID = 0

const DKID ComponentID = 0

const DLID ComponentID = 0

const DMID ComponentID = 0

const DNID ComponentID = 0

const DOID ComponentID = 0

const DPID ComponentID = 0

const DQID ComponentID = 0

const DRID ComponentID = 0

const DSID ComponentID = 0

const DTID ComponentID = 0

const DUID ComponentID = 0

const DVID ComponentID = 0

const DWID ComponentID = 0

const DXID ComponentID = 0

const DYID ComponentID = 0

const DZID ComponentID = 0

const EAID ComponentID = 0

const EBID ComponentID = 0

const ECID ComponentID = 0

const EDID ComponentID = 0

const EEID ComponentID = 0

const EFID ComponentID = 0

const EGID ComponentID = 0

const EHID ComponentID = 0

const EIID ComponentID = 0

const EJID ComponentID = 0

const EKID ComponentID = 0

const ELID ComponentID = 0

const EMID ComponentID = 0

const ENID ComponentID = 0

const EOID ComponentID = 0

const EPID ComponentID = 0

const EQID ComponentID = 0

const ERID ComponentID = 0

const ESID ComponentID = 0

const ETID ComponentID = 0



func (e Entity) SetPosition(c comp.Position) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 1
    storePosition[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetVelocity(c comp.Velocity) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 2
    storeVelocity[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetA(c comp.A) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 4
    storeA[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetB(c comp.B) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 8
    storeB[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetC(c comp.C) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 16
    storeC[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetD(c comp.D) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 32
    storeD[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetE(c comp.E) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 64
    storeE[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetF(c comp.F) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 128
    storeF[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetG(c comp.G) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 256
    storeG[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetH(c comp.H) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 512
    storeH[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetI(c comp.I) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 1024
    storeI[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetJ(c comp.J) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 2048
    storeJ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetK(c comp.K) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 4096
    storeK[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetL(c comp.L) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 8192
    storeL[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetM(c comp.M) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 16384
    storeM[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetN(c comp.N) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 32768
    storeN[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetO(c comp.O) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 65536
    storeO[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetP(c comp.P) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 131072
    storeP[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetQ(c comp.Q) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 262144
    storeQ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetR(c comp.R) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 524288
    storeR[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetS(c comp.S) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 1048576
    storeS[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetT(c comp.T) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 2097152
    storeT[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetU(c comp.U) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 4194304
    storeU[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetV(c comp.V) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 8388608
    storeV[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetW(c comp.W) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 16777216
    storeW[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetX(c comp.X) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 33554432
    storeX[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetY(c comp.Y) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 67108864
    storeY[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetZ(c comp.Z) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 134217728
    storeZ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAA(c comp.AA) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 268435456
    storeAA[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAB(c comp.AB) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 536870912
    storeAB[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAC(c comp.AC) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 1073741824
    storeAC[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAD(c comp.AD) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 2147483648
    storeAD[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAE(c comp.AE) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 4294967296
    storeAE[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAF(c comp.AF) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 8589934592
    storeAF[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAG(c comp.AG) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 17179869184
    storeAG[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAH(c comp.AH) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 34359738368
    storeAH[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAI(c comp.AI) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 68719476736
    storeAI[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAJ(c comp.AJ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 137438953472
    storeAJ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAK(c comp.AK) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 274877906944
    storeAK[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAL(c comp.AL) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 549755813888
    storeAL[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAM(c comp.AM) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 1099511627776
    storeAM[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAN(c comp.AN) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 2199023255552
    storeAN[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAO(c comp.AO) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 4398046511104
    storeAO[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAP(c comp.AP) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 8796093022208
    storeAP[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAQ(c comp.AQ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 17592186044416
    storeAQ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAR(c comp.AR) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 35184372088832
    storeAR[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAS(c comp.AS) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 70368744177664
    storeAS[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAT(c comp.AT) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 140737488355328
    storeAT[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAU(c comp.AU) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 281474976710656
    storeAU[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAV(c comp.AV) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 562949953421312
    storeAV[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAW(c comp.AW) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 1125899906842624
    storeAW[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAX(c comp.AX) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 2251799813685248
    storeAX[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAY(c comp.AY) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 4503599627370496
    storeAY[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetAZ(c comp.AZ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 9007199254740992
    storeAZ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBA(c comp.BA) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 18014398509481984
    storeBA[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBB(c comp.BB) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 36028797018963968
    storeBB[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBC(c comp.BC) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 72057594037927936
    storeBC[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBD(c comp.BD) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 144115188075855872
    storeBD[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBE(c comp.BE) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 288230376151711744
    storeBE[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBF(c comp.BF) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 576460752303423488
    storeBF[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBG(c comp.BG) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 1152921504606846976
    storeBG[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBH(c comp.BH) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 2305843009213693952
    storeBH[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBI(c comp.BI) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= 4611686018427387904
    storeBI[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBJ(c comp.BJ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] |= -9223372036854775808
    storeBJ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBK(c comp.BK) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 1
    storeBK[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBL(c comp.BL) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 2
    storeBL[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBM(c comp.BM) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 4
    storeBM[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBN(c comp.BN) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 8
    storeBN[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBO(c comp.BO) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 16
    storeBO[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBP(c comp.BP) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 32
    storeBP[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBQ(c comp.BQ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 64
    storeBQ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBR(c comp.BR) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 128
    storeBR[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBS(c comp.BS) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 256
    storeBS[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBT(c comp.BT) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 512
    storeBT[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBU(c comp.BU) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 1024
    storeBU[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBV(c comp.BV) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 2048
    storeBV[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBW(c comp.BW) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 4096
    storeBW[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBX(c comp.BX) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 8192
    storeBX[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBY(c comp.BY) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 16384
    storeBY[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetBZ(c comp.BZ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 32768
    storeBZ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCA(c comp.CA) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 65536
    storeCA[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCB(c comp.CB) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 131072
    storeCB[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCC(c comp.CC) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 262144
    storeCC[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCD(c comp.CD) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 524288
    storeCD[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCE(c comp.CE) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 1048576
    storeCE[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCF(c comp.CF) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 2097152
    storeCF[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCG(c comp.CG) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 4194304
    storeCG[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCH(c comp.CH) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 8388608
    storeCH[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCI(c comp.CI) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 16777216
    storeCI[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCJ(c comp.CJ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 33554432
    storeCJ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCK(c comp.CK) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 67108864
    storeCK[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCL(c comp.CL) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 134217728
    storeCL[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCM(c comp.CM) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 268435456
    storeCM[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCN(c comp.CN) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 536870912
    storeCN[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCO(c comp.CO) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 1073741824
    storeCO[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCP(c comp.CP) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 2147483648
    storeCP[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCQ(c comp.CQ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 4294967296
    storeCQ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCR(c comp.CR) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 8589934592
    storeCR[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCS(c comp.CS) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 17179869184
    storeCS[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCT(c comp.CT) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 34359738368
    storeCT[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCU(c comp.CU) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 68719476736
    storeCU[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCV(c comp.CV) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 137438953472
    storeCV[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCW(c comp.CW) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 274877906944
    storeCW[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCX(c comp.CX) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 549755813888
    storeCX[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCY(c comp.CY) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 1099511627776
    storeCY[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetCZ(c comp.CZ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 2199023255552
    storeCZ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDA(c comp.DA) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 4398046511104
    storeDA[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDB(c comp.DB) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 8796093022208
    storeDB[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDC(c comp.DC) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 17592186044416
    storeDC[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDD(c comp.DD) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 35184372088832
    storeDD[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDE(c comp.DE) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 70368744177664
    storeDE[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDF(c comp.DF) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 140737488355328
    storeDF[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDG(c comp.DG) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 281474976710656
    storeDG[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDH(c comp.DH) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 562949953421312
    storeDH[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDI(c comp.DI) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 1125899906842624
    storeDI[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDJ(c comp.DJ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 2251799813685248
    storeDJ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDK(c comp.DK) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 4503599627370496
    storeDK[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDL(c comp.DL) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 9007199254740992
    storeDL[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDM(c comp.DM) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 18014398509481984
    storeDM[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDN(c comp.DN) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 36028797018963968
    storeDN[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDO(c comp.DO) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 72057594037927936
    storeDO[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDP(c comp.DP) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 144115188075855872
    storeDP[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDQ(c comp.DQ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 288230376151711744
    storeDQ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDR(c comp.DR) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 576460752303423488
    storeDR[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDS(c comp.DS) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 1152921504606846976
    storeDS[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDT(c comp.DT) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 2305843009213693952
    storeDT[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDU(c comp.DU) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= 4611686018427387904
    storeDU[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDV(c comp.DV) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] |= -9223372036854775808
    storeDV[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDW(c comp.DW) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 1
    storeDW[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDX(c comp.DX) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 2
    storeDX[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDY(c comp.DY) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 4
    storeDY[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetDZ(c comp.DZ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 8
    storeDZ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEA(c comp.EA) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 16
    storeEA[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEB(c comp.EB) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 32
    storeEB[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEC(c comp.EC) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 64
    storeEC[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetED(c comp.ED) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 128
    storeED[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEE(c comp.EE) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 256
    storeEE[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEF(c comp.EF) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 512
    storeEF[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEG(c comp.EG) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 1024
    storeEG[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEH(c comp.EH) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 2048
    storeEH[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEI(c comp.EI) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 4096
    storeEI[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEJ(c comp.EJ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 8192
    storeEJ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEK(c comp.EK) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 16384
    storeEK[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEL(c comp.EL) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 32768
    storeEL[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEM(c comp.EM) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 65536
    storeEM[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEN(c comp.EN) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 131072
    storeEN[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEO(c comp.EO) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 262144
    storeEO[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEP(c comp.EP) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 524288
    storeEP[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetEQ(c comp.EQ) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 1048576
    storeEQ[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetER(c comp.ER) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 2097152
    storeER[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetES(c comp.ES) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 4194304
    storeES[e.id >> entityPageBits][e.id % entityPageSize] = c
}

func (e Entity) SetET(c comp.ET) {
    if !e.Alive() {
        return
    }

    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] |= 8388608
    storeET[e.id >> entityPageBits][e.id % entityPageSize] = c
}



func (e Entity) RemovePosition() {
    if !e.Alive() {
        return
    }

    var zero comp.Position
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(1)
    storePosition[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveVelocity() {
    if !e.Alive() {
        return
    }

    var zero comp.Velocity
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(2)
    storeVelocity[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveA() {
    if !e.Alive() {
        return
    }

    var zero comp.A
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(4)
    storeA[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveB() {
    if !e.Alive() {
        return
    }

    var zero comp.B
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(8)
    storeB[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveC() {
    if !e.Alive() {
        return
    }

    var zero comp.C
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(16)
    storeC[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveD() {
    if !e.Alive() {
        return
    }

    var zero comp.D
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(32)
    storeD[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveE() {
    if !e.Alive() {
        return
    }

    var zero comp.E
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(64)
    storeE[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveF() {
    if !e.Alive() {
        return
    }

    var zero comp.F
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(128)
    storeF[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveG() {
    if !e.Alive() {
        return
    }

    var zero comp.G
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(256)
    storeG[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveH() {
    if !e.Alive() {
        return
    }

    var zero comp.H
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(512)
    storeH[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveI() {
    if !e.Alive() {
        return
    }

    var zero comp.I
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(1024)
    storeI[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveJ() {
    if !e.Alive() {
        return
    }

    var zero comp.J
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(2048)
    storeJ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveK() {
    if !e.Alive() {
        return
    }

    var zero comp.K
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(4096)
    storeK[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveL() {
    if !e.Alive() {
        return
    }

    var zero comp.L
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(8192)
    storeL[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveM() {
    if !e.Alive() {
        return
    }

    var zero comp.M
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(16384)
    storeM[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveN() {
    if !e.Alive() {
        return
    }

    var zero comp.N
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(32768)
    storeN[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveO() {
    if !e.Alive() {
        return
    }

    var zero comp.O
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(65536)
    storeO[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveP() {
    if !e.Alive() {
        return
    }

    var zero comp.P
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(131072)
    storeP[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveQ() {
    if !e.Alive() {
        return
    }

    var zero comp.Q
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(262144)
    storeQ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveR() {
    if !e.Alive() {
        return
    }

    var zero comp.R
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(524288)
    storeR[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveS() {
    if !e.Alive() {
        return
    }

    var zero comp.S
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(1048576)
    storeS[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveT() {
    if !e.Alive() {
        return
    }

    var zero comp.T
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(2097152)
    storeT[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveU() {
    if !e.Alive() {
        return
    }

    var zero comp.U
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(4194304)
    storeU[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveV() {
    if !e.Alive() {
        return
    }

    var zero comp.V
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(8388608)
    storeV[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveW() {
    if !e.Alive() {
        return
    }

    var zero comp.W
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(16777216)
    storeW[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveX() {
    if !e.Alive() {
        return
    }

    var zero comp.X
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(33554432)
    storeX[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveY() {
    if !e.Alive() {
        return
    }

    var zero comp.Y
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(67108864)
    storeY[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveZ() {
    if !e.Alive() {
        return
    }

    var zero comp.Z
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(134217728)
    storeZ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAA() {
    if !e.Alive() {
        return
    }

    var zero comp.AA
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(268435456)
    storeAA[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAB() {
    if !e.Alive() {
        return
    }

    var zero comp.AB
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(536870912)
    storeAB[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAC() {
    if !e.Alive() {
        return
    }

    var zero comp.AC
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(1073741824)
    storeAC[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAD() {
    if !e.Alive() {
        return
    }

    var zero comp.AD
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(2147483648)
    storeAD[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAE() {
    if !e.Alive() {
        return
    }

    var zero comp.AE
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(4294967296)
    storeAE[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAF() {
    if !e.Alive() {
        return
    }

    var zero comp.AF
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(8589934592)
    storeAF[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAG() {
    if !e.Alive() {
        return
    }

    var zero comp.AG
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(17179869184)
    storeAG[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAH() {
    if !e.Alive() {
        return
    }

    var zero comp.AH
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(34359738368)
    storeAH[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAI() {
    if !e.Alive() {
        return
    }

    var zero comp.AI
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(68719476736)
    storeAI[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAJ() {
    if !e.Alive() {
        return
    }

    var zero comp.AJ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(137438953472)
    storeAJ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAK() {
    if !e.Alive() {
        return
    }

    var zero comp.AK
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(274877906944)
    storeAK[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAL() {
    if !e.Alive() {
        return
    }

    var zero comp.AL
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(549755813888)
    storeAL[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAM() {
    if !e.Alive() {
        return
    }

    var zero comp.AM
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(1099511627776)
    storeAM[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAN() {
    if !e.Alive() {
        return
    }

    var zero comp.AN
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(2199023255552)
    storeAN[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAO() {
    if !e.Alive() {
        return
    }

    var zero comp.AO
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(4398046511104)
    storeAO[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAP() {
    if !e.Alive() {
        return
    }

    var zero comp.AP
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(8796093022208)
    storeAP[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAQ() {
    if !e.Alive() {
        return
    }

    var zero comp.AQ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(17592186044416)
    storeAQ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAR() {
    if !e.Alive() {
        return
    }

    var zero comp.AR
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(35184372088832)
    storeAR[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAS() {
    if !e.Alive() {
        return
    }

    var zero comp.AS
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(70368744177664)
    storeAS[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAT() {
    if !e.Alive() {
        return
    }

    var zero comp.AT
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(140737488355328)
    storeAT[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAU() {
    if !e.Alive() {
        return
    }

    var zero comp.AU
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(281474976710656)
    storeAU[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAV() {
    if !e.Alive() {
        return
    }

    var zero comp.AV
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(562949953421312)
    storeAV[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAW() {
    if !e.Alive() {
        return
    }

    var zero comp.AW
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(1125899906842624)
    storeAW[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAX() {
    if !e.Alive() {
        return
    }

    var zero comp.AX
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(2251799813685248)
    storeAX[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAY() {
    if !e.Alive() {
        return
    }

    var zero comp.AY
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(4503599627370496)
    storeAY[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveAZ() {
    if !e.Alive() {
        return
    }

    var zero comp.AZ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(9007199254740992)
    storeAZ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBA() {
    if !e.Alive() {
        return
    }

    var zero comp.BA
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(18014398509481984)
    storeBA[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBB() {
    if !e.Alive() {
        return
    }

    var zero comp.BB
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(36028797018963968)
    storeBB[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBC() {
    if !e.Alive() {
        return
    }

    var zero comp.BC
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(72057594037927936)
    storeBC[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBD() {
    if !e.Alive() {
        return
    }

    var zero comp.BD
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(144115188075855872)
    storeBD[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBE() {
    if !e.Alive() {
        return
    }

    var zero comp.BE
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(288230376151711744)
    storeBE[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBF() {
    if !e.Alive() {
        return
    }

    var zero comp.BF
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(576460752303423488)
    storeBF[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBG() {
    if !e.Alive() {
        return
    }

    var zero comp.BG
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(1152921504606846976)
    storeBG[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBH() {
    if !e.Alive() {
        return
    }

    var zero comp.BH
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(2305843009213693952)
    storeBH[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBI() {
    if !e.Alive() {
        return
    }

    var zero comp.BI
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(4611686018427387904)
    storeBI[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBJ() {
    if !e.Alive() {
        return
    }

    var zero comp.BJ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] &= ^uint64(-9223372036854775808)
    storeBJ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBK() {
    if !e.Alive() {
        return
    }

    var zero comp.BK
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(1)
    storeBK[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBL() {
    if !e.Alive() {
        return
    }

    var zero comp.BL
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(2)
    storeBL[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBM() {
    if !e.Alive() {
        return
    }

    var zero comp.BM
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(4)
    storeBM[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBN() {
    if !e.Alive() {
        return
    }

    var zero comp.BN
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(8)
    storeBN[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBO() {
    if !e.Alive() {
        return
    }

    var zero comp.BO
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(16)
    storeBO[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBP() {
    if !e.Alive() {
        return
    }

    var zero comp.BP
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(32)
    storeBP[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBQ() {
    if !e.Alive() {
        return
    }

    var zero comp.BQ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(64)
    storeBQ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBR() {
    if !e.Alive() {
        return
    }

    var zero comp.BR
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(128)
    storeBR[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBS() {
    if !e.Alive() {
        return
    }

    var zero comp.BS
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(256)
    storeBS[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBT() {
    if !e.Alive() {
        return
    }

    var zero comp.BT
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(512)
    storeBT[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBU() {
    if !e.Alive() {
        return
    }

    var zero comp.BU
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(1024)
    storeBU[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBV() {
    if !e.Alive() {
        return
    }

    var zero comp.BV
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(2048)
    storeBV[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBW() {
    if !e.Alive() {
        return
    }

    var zero comp.BW
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(4096)
    storeBW[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBX() {
    if !e.Alive() {
        return
    }

    var zero comp.BX
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(8192)
    storeBX[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBY() {
    if !e.Alive() {
        return
    }

    var zero comp.BY
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(16384)
    storeBY[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveBZ() {
    if !e.Alive() {
        return
    }

    var zero comp.BZ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(32768)
    storeBZ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCA() {
    if !e.Alive() {
        return
    }

    var zero comp.CA
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(65536)
    storeCA[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCB() {
    if !e.Alive() {
        return
    }

    var zero comp.CB
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(131072)
    storeCB[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCC() {
    if !e.Alive() {
        return
    }

    var zero comp.CC
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(262144)
    storeCC[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCD() {
    if !e.Alive() {
        return
    }

    var zero comp.CD
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(524288)
    storeCD[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCE() {
    if !e.Alive() {
        return
    }

    var zero comp.CE
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(1048576)
    storeCE[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCF() {
    if !e.Alive() {
        return
    }

    var zero comp.CF
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(2097152)
    storeCF[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCG() {
    if !e.Alive() {
        return
    }

    var zero comp.CG
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(4194304)
    storeCG[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCH() {
    if !e.Alive() {
        return
    }

    var zero comp.CH
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(8388608)
    storeCH[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCI() {
    if !e.Alive() {
        return
    }

    var zero comp.CI
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(16777216)
    storeCI[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCJ() {
    if !e.Alive() {
        return
    }

    var zero comp.CJ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(33554432)
    storeCJ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCK() {
    if !e.Alive() {
        return
    }

    var zero comp.CK
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(67108864)
    storeCK[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCL() {
    if !e.Alive() {
        return
    }

    var zero comp.CL
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(134217728)
    storeCL[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCM() {
    if !e.Alive() {
        return
    }

    var zero comp.CM
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(268435456)
    storeCM[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCN() {
    if !e.Alive() {
        return
    }

    var zero comp.CN
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(536870912)
    storeCN[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCO() {
    if !e.Alive() {
        return
    }

    var zero comp.CO
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(1073741824)
    storeCO[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCP() {
    if !e.Alive() {
        return
    }

    var zero comp.CP
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(2147483648)
    storeCP[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCQ() {
    if !e.Alive() {
        return
    }

    var zero comp.CQ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(4294967296)
    storeCQ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCR() {
    if !e.Alive() {
        return
    }

    var zero comp.CR
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(8589934592)
    storeCR[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCS() {
    if !e.Alive() {
        return
    }

    var zero comp.CS
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(17179869184)
    storeCS[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCT() {
    if !e.Alive() {
        return
    }

    var zero comp.CT
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(34359738368)
    storeCT[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCU() {
    if !e.Alive() {
        return
    }

    var zero comp.CU
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(68719476736)
    storeCU[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCV() {
    if !e.Alive() {
        return
    }

    var zero comp.CV
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(137438953472)
    storeCV[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCW() {
    if !e.Alive() {
        return
    }

    var zero comp.CW
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(274877906944)
    storeCW[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCX() {
    if !e.Alive() {
        return
    }

    var zero comp.CX
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(549755813888)
    storeCX[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCY() {
    if !e.Alive() {
        return
    }

    var zero comp.CY
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(1099511627776)
    storeCY[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveCZ() {
    if !e.Alive() {
        return
    }

    var zero comp.CZ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(2199023255552)
    storeCZ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDA() {
    if !e.Alive() {
        return
    }

    var zero comp.DA
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(4398046511104)
    storeDA[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDB() {
    if !e.Alive() {
        return
    }

    var zero comp.DB
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(8796093022208)
    storeDB[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDC() {
    if !e.Alive() {
        return
    }

    var zero comp.DC
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(17592186044416)
    storeDC[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDD() {
    if !e.Alive() {
        return
    }

    var zero comp.DD
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(35184372088832)
    storeDD[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDE() {
    if !e.Alive() {
        return
    }

    var zero comp.DE
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(70368744177664)
    storeDE[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDF() {
    if !e.Alive() {
        return
    }

    var zero comp.DF
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(140737488355328)
    storeDF[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDG() {
    if !e.Alive() {
        return
    }

    var zero comp.DG
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(281474976710656)
    storeDG[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDH() {
    if !e.Alive() {
        return
    }

    var zero comp.DH
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(562949953421312)
    storeDH[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDI() {
    if !e.Alive() {
        return
    }

    var zero comp.DI
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(1125899906842624)
    storeDI[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDJ() {
    if !e.Alive() {
        return
    }

    var zero comp.DJ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(2251799813685248)
    storeDJ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDK() {
    if !e.Alive() {
        return
    }

    var zero comp.DK
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(4503599627370496)
    storeDK[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDL() {
    if !e.Alive() {
        return
    }

    var zero comp.DL
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(9007199254740992)
    storeDL[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDM() {
    if !e.Alive() {
        return
    }

    var zero comp.DM
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(18014398509481984)
    storeDM[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDN() {
    if !e.Alive() {
        return
    }

    var zero comp.DN
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(36028797018963968)
    storeDN[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDO() {
    if !e.Alive() {
        return
    }

    var zero comp.DO
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(72057594037927936)
    storeDO[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDP() {
    if !e.Alive() {
        return
    }

    var zero comp.DP
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(144115188075855872)
    storeDP[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDQ() {
    if !e.Alive() {
        return
    }

    var zero comp.DQ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(288230376151711744)
    storeDQ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDR() {
    if !e.Alive() {
        return
    }

    var zero comp.DR
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(576460752303423488)
    storeDR[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDS() {
    if !e.Alive() {
        return
    }

    var zero comp.DS
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(1152921504606846976)
    storeDS[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDT() {
    if !e.Alive() {
        return
    }

    var zero comp.DT
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(2305843009213693952)
    storeDT[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDU() {
    if !e.Alive() {
        return
    }

    var zero comp.DU
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(4611686018427387904)
    storeDU[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDV() {
    if !e.Alive() {
        return
    }

    var zero comp.DV
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] &= ^uint64(-9223372036854775808)
    storeDV[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDW() {
    if !e.Alive() {
        return
    }

    var zero comp.DW
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(1)
    storeDW[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDX() {
    if !e.Alive() {
        return
    }

    var zero comp.DX
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(2)
    storeDX[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDY() {
    if !e.Alive() {
        return
    }

    var zero comp.DY
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(4)
    storeDY[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveDZ() {
    if !e.Alive() {
        return
    }

    var zero comp.DZ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(8)
    storeDZ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEA() {
    if !e.Alive() {
        return
    }

    var zero comp.EA
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(16)
    storeEA[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEB() {
    if !e.Alive() {
        return
    }

    var zero comp.EB
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(32)
    storeEB[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEC() {
    if !e.Alive() {
        return
    }

    var zero comp.EC
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(64)
    storeEC[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveED() {
    if !e.Alive() {
        return
    }

    var zero comp.ED
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(128)
    storeED[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEE() {
    if !e.Alive() {
        return
    }

    var zero comp.EE
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(256)
    storeEE[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEF() {
    if !e.Alive() {
        return
    }

    var zero comp.EF
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(512)
    storeEF[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEG() {
    if !e.Alive() {
        return
    }

    var zero comp.EG
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(1024)
    storeEG[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEH() {
    if !e.Alive() {
        return
    }

    var zero comp.EH
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(2048)
    storeEH[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEI() {
    if !e.Alive() {
        return
    }

    var zero comp.EI
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(4096)
    storeEI[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEJ() {
    if !e.Alive() {
        return
    }

    var zero comp.EJ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(8192)
    storeEJ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEK() {
    if !e.Alive() {
        return
    }

    var zero comp.EK
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(16384)
    storeEK[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEL() {
    if !e.Alive() {
        return
    }

    var zero comp.EL
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(32768)
    storeEL[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEM() {
    if !e.Alive() {
        return
    }

    var zero comp.EM
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(65536)
    storeEM[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEN() {
    if !e.Alive() {
        return
    }

    var zero comp.EN
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(131072)
    storeEN[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEO() {
    if !e.Alive() {
        return
    }

    var zero comp.EO
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(262144)
    storeEO[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEP() {
    if !e.Alive() {
        return
    }

    var zero comp.EP
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(524288)
    storeEP[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveEQ() {
    if !e.Alive() {
        return
    }

    var zero comp.EQ
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(1048576)
    storeEQ[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveER() {
    if !e.Alive() {
        return
    }

    var zero comp.ER
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(2097152)
    storeER[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveES() {
    if !e.Alive() {
        return
    }

    var zero comp.ES
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(4194304)
    storeES[e.id >> entityPageBits][e.id % entityPageSize] = zero
}

func (e Entity) RemoveET() {
    if !e.Alive() {
        return
    }

    var zero comp.ET
    entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] &= ^uint64(8388608)
    storeET[e.id >> entityPageBits][e.id % entityPageSize] = zero
}



func (e Entity) HasPosition() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 1 != 0
}

func (e Entity) HasVelocity() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 2 != 0
}

func (e Entity) HasA() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 4 != 0
}

func (e Entity) HasB() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 8 != 0
}

func (e Entity) HasC() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 16 != 0
}

func (e Entity) HasD() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 32 != 0
}

func (e Entity) HasE() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 64 != 0
}

func (e Entity) HasF() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 128 != 0
}

func (e Entity) HasG() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 256 != 0
}

func (e Entity) HasH() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 512 != 0
}

func (e Entity) HasI() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 1024 != 0
}

func (e Entity) HasJ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 2048 != 0
}

func (e Entity) HasK() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 4096 != 0
}

func (e Entity) HasL() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 8192 != 0
}

func (e Entity) HasM() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 16384 != 0
}

func (e Entity) HasN() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 32768 != 0
}

func (e Entity) HasO() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 65536 != 0
}

func (e Entity) HasP() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 131072 != 0
}

func (e Entity) HasQ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 262144 != 0
}

func (e Entity) HasR() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 524288 != 0
}

func (e Entity) HasS() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 1048576 != 0
}

func (e Entity) HasT() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 2097152 != 0
}

func (e Entity) HasU() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 4194304 != 0
}

func (e Entity) HasV() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 8388608 != 0
}

func (e Entity) HasW() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 16777216 != 0
}

func (e Entity) HasX() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 33554432 != 0
}

func (e Entity) HasY() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 67108864 != 0
}

func (e Entity) HasZ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 134217728 != 0
}

func (e Entity) HasAA() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 268435456 != 0
}

func (e Entity) HasAB() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 536870912 != 0
}

func (e Entity) HasAC() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 1073741824 != 0
}

func (e Entity) HasAD() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 2147483648 != 0
}

func (e Entity) HasAE() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 4294967296 != 0
}

func (e Entity) HasAF() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 8589934592 != 0
}

func (e Entity) HasAG() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 17179869184 != 0
}

func (e Entity) HasAH() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 34359738368 != 0
}

func (e Entity) HasAI() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 68719476736 != 0
}

func (e Entity) HasAJ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 137438953472 != 0
}

func (e Entity) HasAK() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 274877906944 != 0
}

func (e Entity) HasAL() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 549755813888 != 0
}

func (e Entity) HasAM() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 1099511627776 != 0
}

func (e Entity) HasAN() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 2199023255552 != 0
}

func (e Entity) HasAO() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 4398046511104 != 0
}

func (e Entity) HasAP() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 8796093022208 != 0
}

func (e Entity) HasAQ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 17592186044416 != 0
}

func (e Entity) HasAR() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 35184372088832 != 0
}

func (e Entity) HasAS() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 70368744177664 != 0
}

func (e Entity) HasAT() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 140737488355328 != 0
}

func (e Entity) HasAU() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 281474976710656 != 0
}

func (e Entity) HasAV() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 562949953421312 != 0
}

func (e Entity) HasAW() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 1125899906842624 != 0
}

func (e Entity) HasAX() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 2251799813685248 != 0
}

func (e Entity) HasAY() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 4503599627370496 != 0
}

func (e Entity) HasAZ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 9007199254740992 != 0
}

func (e Entity) HasBA() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 18014398509481984 != 0
}

func (e Entity) HasBB() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 36028797018963968 != 0
}

func (e Entity) HasBC() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 72057594037927936 != 0
}

func (e Entity) HasBD() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 144115188075855872 != 0
}

func (e Entity) HasBE() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 288230376151711744 != 0
}

func (e Entity) HasBF() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 576460752303423488 != 0
}

func (e Entity) HasBG() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 1152921504606846976 != 0
}

func (e Entity) HasBH() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 2305843009213693952 != 0
}

func (e Entity) HasBI() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & 4611686018427387904 != 0
}

func (e Entity) HasBJ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[0] & -9223372036854775808 != 0
}

func (e Entity) HasBK() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 1 != 0
}

func (e Entity) HasBL() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 2 != 0
}

func (e Entity) HasBM() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 4 != 0
}

func (e Entity) HasBN() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 8 != 0
}

func (e Entity) HasBO() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 16 != 0
}

func (e Entity) HasBP() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 32 != 0
}

func (e Entity) HasBQ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 64 != 0
}

func (e Entity) HasBR() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 128 != 0
}

func (e Entity) HasBS() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 256 != 0
}

func (e Entity) HasBT() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 512 != 0
}

func (e Entity) HasBU() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 1024 != 0
}

func (e Entity) HasBV() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 2048 != 0
}

func (e Entity) HasBW() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 4096 != 0
}

func (e Entity) HasBX() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 8192 != 0
}

func (e Entity) HasBY() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 16384 != 0
}

func (e Entity) HasBZ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 32768 != 0
}

func (e Entity) HasCA() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 65536 != 0
}

func (e Entity) HasCB() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 131072 != 0
}

func (e Entity) HasCC() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 262144 != 0
}

func (e Entity) HasCD() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 524288 != 0
}

func (e Entity) HasCE() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 1048576 != 0
}

func (e Entity) HasCF() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 2097152 != 0
}

func (e Entity) HasCG() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 4194304 != 0
}

func (e Entity) HasCH() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 8388608 != 0
}

func (e Entity) HasCI() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 16777216 != 0
}

func (e Entity) HasCJ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 33554432 != 0
}

func (e Entity) HasCK() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 67108864 != 0
}

func (e Entity) HasCL() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 134217728 != 0
}

func (e Entity) HasCM() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 268435456 != 0
}

func (e Entity) HasCN() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 536870912 != 0
}

func (e Entity) HasCO() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 1073741824 != 0
}

func (e Entity) HasCP() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 2147483648 != 0
}

func (e Entity) HasCQ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 4294967296 != 0
}

func (e Entity) HasCR() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 8589934592 != 0
}

func (e Entity) HasCS() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 17179869184 != 0
}

func (e Entity) HasCT() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 34359738368 != 0
}

func (e Entity) HasCU() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 68719476736 != 0
}

func (e Entity) HasCV() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 137438953472 != 0
}

func (e Entity) HasCW() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 274877906944 != 0
}

func (e Entity) HasCX() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 549755813888 != 0
}

func (e Entity) HasCY() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 1099511627776 != 0
}

func (e Entity) HasCZ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 2199023255552 != 0
}

func (e Entity) HasDA() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 4398046511104 != 0
}

func (e Entity) HasDB() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 8796093022208 != 0
}

func (e Entity) HasDC() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 17592186044416 != 0
}

func (e Entity) HasDD() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 35184372088832 != 0
}

func (e Entity) HasDE() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 70368744177664 != 0
}

func (e Entity) HasDF() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 140737488355328 != 0
}

func (e Entity) HasDG() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 281474976710656 != 0
}

func (e Entity) HasDH() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 562949953421312 != 0
}

func (e Entity) HasDI() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 1125899906842624 != 0
}

func (e Entity) HasDJ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 2251799813685248 != 0
}

func (e Entity) HasDK() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 4503599627370496 != 0
}

func (e Entity) HasDL() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 9007199254740992 != 0
}

func (e Entity) HasDM() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 18014398509481984 != 0
}

func (e Entity) HasDN() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 36028797018963968 != 0
}

func (e Entity) HasDO() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 72057594037927936 != 0
}

func (e Entity) HasDP() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 144115188075855872 != 0
}

func (e Entity) HasDQ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 288230376151711744 != 0
}

func (e Entity) HasDR() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 576460752303423488 != 0
}

func (e Entity) HasDS() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 1152921504606846976 != 0
}

func (e Entity) HasDT() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 2305843009213693952 != 0
}

func (e Entity) HasDU() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & 4611686018427387904 != 0
}

func (e Entity) HasDV() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[1] & -9223372036854775808 != 0
}

func (e Entity) HasDW() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 1 != 0
}

func (e Entity) HasDX() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 2 != 0
}

func (e Entity) HasDY() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 4 != 0
}

func (e Entity) HasDZ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 8 != 0
}

func (e Entity) HasEA() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 16 != 0
}

func (e Entity) HasEB() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 32 != 0
}

func (e Entity) HasEC() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 64 != 0
}

func (e Entity) HasED() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 128 != 0
}

func (e Entity) HasEE() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 256 != 0
}

func (e Entity) HasEF() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 512 != 0
}

func (e Entity) HasEG() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 1024 != 0
}

func (e Entity) HasEH() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 2048 != 0
}

func (e Entity) HasEI() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 4096 != 0
}

func (e Entity) HasEJ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 8192 != 0
}

func (e Entity) HasEK() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 16384 != 0
}

func (e Entity) HasEL() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 32768 != 0
}

func (e Entity) HasEM() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 65536 != 0
}

func (e Entity) HasEN() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 131072 != 0
}

func (e Entity) HasEO() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 262144 != 0
}

func (e Entity) HasEP() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 524288 != 0
}

func (e Entity) HasEQ() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 1048576 != 0
}

func (e Entity) HasER() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 2097152 != 0
}

func (e Entity) HasES() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 4194304 != 0
}

func (e Entity) HasET() bool {
    if !e.Alive() {
        return false
    }
    return entities[e.id >> entityPageBits][e.id % entityPageSize].components[2] & 8388608 != 0
}

